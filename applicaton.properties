# ===============================================
# \u2705 SERVER CONFIGURATION
# ===============================================
server.port=8080
spring.application.name=country-exchange-api

# ===============================================
# \u2705 DATABASE CONFIGURATION (MySQL)
# ===============================================
# Added: allowPublicKeyRetrieval=true to fix authentication issue
spring.datasource.url=jdbc:mysql://localhost:3306/country_exchange_db?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
spring.datasource.username=Phangi
spring.datasource.password=686691Pm.
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# ===============================================
# \u2705 JPA / HIBERNATE CONFIGURATION
# ===============================================
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.database-platform=org.hibernate.dialect.MySQL8Dialect

# ===============================================
# \u2705 LOGGING CONFIGURATION
# ===============================================
logging.level.org.springframework=INFO
logging.level.com.example.country=DEBUG

# ===============================================
# \u2705 EXTERNAL API (OPTIONAL)
# If your app calls an external exchange rate API, configure it here:
# country.api.url=https://api.exchangerate.host/latest
